var partitionLabels = function(s) {
    //Create an empty array to return count
    //Initialize a value to store the length of the input string
    //Loop through the array
    //Split into equal parts
    //If its even, divide equally using a multiple of two
    //Else
    //Count the length of each splitted string
    //Unshift the length of string to empty array for count
    
    let count = []
    let stringLen = s.length
    for (let i = 0; i < s.length; i++){
        let stringLen = s.length
        if (stringLen % 2 === 0){
           // let splitted = s.split(" ")
        }
    }
    let splitted = s.split(" ")

    console.log(splitted)
    
};

partitionLabels("abcdfrhhhh")

